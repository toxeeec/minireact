{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "demo",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild index.html src/index.ts --bundle --watch --outdir=dist --serve=3000 --loader:.html=copy"
  },
  "dependencies": {
    "minireact": "workspace:*"
  }
}
